import { useState } from 'react';
import { Plus, Minus } from 'lucide-react';
import { FAQS } from '@/data';
export default function FAQ() { const [open, setOpen] = useState(0); return <section id="faq" className="fl-section bg-cream-100/60"><div className="fl-container max-w-3xl"><div className="text-center"><span className="fl-eyebrow text-saffron-600">Questions, answered</span><h2 className="mt-3 font-display text-[clamp(1.7rem,5vw,2.8rem)] text-forest-900">Everything you need to know</h2></div><div className="mt-10 space-y-3">{FAQS.map((item, index) => { const active = open === index; return <div key={item.q} className="fl-card overflow-hidden"><button onClick={() => setOpen(active ? -1 : index)} className="flex min-h-16 w-full items-center justify-between gap-4 px-5 py-4 text-left" aria-expanded={active}><span className="font-display text-base text-forest-900 sm:text-lg">{item.q}</span><span className={`grid h-8 w-8 shrink-0 place-items-center rounded-full ${active ? 'bg-saffron-500 text-white' : 'bg-cream-100 text-forest-700'}`}>{active ? <Minus className="h-4 w-4" /> : <Plus className="h-4 w-4" />}</span></button>{active && <p className="px-5 pb-5 text-sm leading-relaxed text-forest-700/75">{item.a}</p>}</div>; })}</div></div></section>; }
