import { useState } from 'react';
import { Check, Sparkles } from 'lucide-react';
import { EFFECTS } from '@/data';
interface Props { selected: string[]; onChange: (ids: string[]) => void; }
export default function EffectLab({ selected, onChange }: Props) { const [group, setGroup] = useState('All'); const groups = ['All', ...Array.from(new Set(EFFECTS.map((effect) => effect.group)))]; const visible = group === 'All' ? EFFECTS : EFFECTS.filter((effect) => effect.group === group); const toggle = (id: string) => onChange(selected.includes(id) ? selected.filter((item) => item !== id) : [...selected, id]); return <section id="effects" className="fl-section bg-forest-900 text-cream-50"><div className="fl-container"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><span className="fl-eyebrow text-gold-300">FestiveLink Effect Lab</span><h2 className="mt-3 font-display text-[clamp(1.8rem,5vw,3rem)]">Make it move.</h2><p className="mt-3 max-w-xl text-cream-100/65">Choose the little moments that turn a beautiful invitation into an experience.</p></div><Sparkles className="hidden h-12 w-12 text-gold-300 sm:block" /></div><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{groups.map((item) => <button key={item} onClick={() => setGroup(item)} className={`fl-chip shrink-0 ${group === item ? '!border-gold-300 !bg-gold-300 !text-forest-900' : '!border-white/20 !bg-white/5 !text-cream-100'}`}>{item}</button>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{visible.map((effect) => <button key={effect.id} onClick={() => toggle(effect.id)} className={`rounded-2xl border p-4 text-left transition-all ${selected.includes(effect.id) ? 'border-gold-300 bg-gold-300/15' : 'border-white/10 bg-white/5 hover:bg-white/10'}`}><div className="flex items-center justify-between"><span className="font-display text-2xl text-gold-300">{effect.icon}</span><span className={`grid h-6 w-6 place-items-center rounded-full ${selected.includes(effect.id) ? 'bg-gold-300 text-forest-900' : 'border border-white/20 text-transparent'}`}><Check className="h-3.5 w-3.5" /></span></div><p className="mt-3 font-semibold">{effect.name}</p><p className="mt-1 text-xs leading-relaxed text-cream-100/55">{effect.description}</p></button>)}</div></div></section>; }
