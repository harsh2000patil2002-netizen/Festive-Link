import { ArrowUpRight } from 'lucide-react';
import { OCCASIONS, type OccasionId } from '@/data';
interface Props { selected: OccasionId; onSelect: (id: OccasionId) => void; }
export default function OccasionGrid({ selected, onSelect }: Props) { return <section id="occasions" className="fl-section"><div className="fl-container"><div className="max-w-2xl"><span className="fl-eyebrow text-saffron-600">Start with a feeling</span><h2 className="mt-3 font-display text-[clamp(1.8rem,5vw,3rem)] text-forest-900">What are you celebrating?</h2><p className="mt-3 max-w-xl text-forest-700/70">From the first hello to the biggest milestone, choose an occasion and make it unmistakably yours.</p></div><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-7">{OCCASIONS.map((occasion) => <button key={occasion.id} onClick={() => onSelect(occasion.id)} className={`group relative min-h-32 rounded-2xl border p-4 text-left transition-all hover:-translate-y-1 hover:shadow-soft ${selected === occasion.id ? 'border-transparent bg-forest-800 text-white shadow-soft' : 'border-forest-900/8 bg-white text-forest-900'}`}><span className="font-display text-2xl" style={{ color: selected === occasion.id ? '#f3c969' : occasion.color }}>{occasion.icon}</span><span className="mt-3 block text-sm font-semibold leading-tight">{occasion.name}</span><ArrowUpRight className="absolute right-3 top-3 h-4 w-4 opacity-0 transition-opacity group-hover:opacity-70" /></button>)}</div></div></section>; }
